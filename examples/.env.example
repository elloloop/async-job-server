# Example environment configuration for async jobs

# Required: PostgreSQL connection string
ASYNC_JOBS_DB_DSN=postgresql://user:password@localhost:5432/asyncjobs

# SQS queue URLs (use your actual AWS SQS queue URLs)
ASYNC_JOBS_SQS_QUEUE_NOTIFICATIONS=https://sqs.us-east-1.amazonaws.com/123456789012/notifications
ASYNC_JOBS_SQS_QUEUE_MESSAGE_LABELING=https://sqs.us-east-1.amazonaws.com/123456789012/message-labeling

# Use case configurations
ASYNC_JOBS_NOTIFICATIONS_MAX_CONCURRENT=10
ASYNC_JOBS_NOTIFICATIONS_DEFAULT_DELAY_TOLERANCE_SECONDS=60

ASYNC_JOBS_MESSAGE_LABELING_MAX_CONCURRENT=10
ASYNC_JOBS_MESSAGE_LABELING_DEFAULT_DELAY_TOLERANCE_SECONDS=60

# Optional: Per-tenant quotas (JSON format)
ASYNC_JOBS_PER_TENANT_QUOTAS={"tenant-123": {"max_pending_jobs": 100}, "tenant-789": {"max_pending_jobs": 50}}

# Optional: Authentication token for API
ASYNC_JOBS_ENQUEUE_AUTH_TOKEN=your-secret-token-here

# Optional: Timing configurations
ASYNC_JOBS_SCHEDULER_INTERVAL_SECONDS=5
ASYNC_JOBS_WORKER_POLL_INTERVAL_SECONDS=1
ASYNC_JOBS_WORKER_VISIBILITY_TIMEOUT_SECONDS=300
ASYNC_JOBS_LEASE_DURATION_SECONDS=300

# AWS credentials (if not using instance roles)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_DEFAULT_REGION=us-east-1

# For LocalStack testing:
# AWS_ENDPOINT_URL=http://localhost:4566
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test
